# Demo fixtures

WAV files in this folder are **not** checked into git.

Generate synthetic placeholder audio locally (procedurally generated tones/clicks — no copyrighted material):

```bash
python examples/generate_fixtures.py
```

This writes `track_01.wav`, `track_02.wav`, and `track_03.wav` for `examples/setlist_example.json`.
