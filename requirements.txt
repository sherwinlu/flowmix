# Core runtime dependencies (mirrors pyproject.toml).
# Prefer: pip install -e .
pydub>=0.25.1
numpy>=1.24
librosa>=0.10
soundfile>=0.12
scipy>=1.10
pyloudnorm>=0.1.1
# Required for pydub on Python 3.13+, where stdlib audioop was removed.
audioop-lts; python_version >= "3.13"
